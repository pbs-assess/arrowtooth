```{r app-comp-env-para-1-en, eval = !fr(), results = 'asis'}
cat("# COMPUTATIONAL ENVIRONMENT

The source code for this assessment is available at:

<https://github.com/pbs-assess/arrowtooth>

This version of the document was generated on `r format(lubridate::as_date(Sys.time()), format = '%a %b, %d')`
with `r devtools::session_info()$platform$version` [@r2022] and R package versions:

")
```

```{r app-comp-env-para-1-fr, eval = fr(), results = 'asis', needs_trans = FALSE}
cat("# ENVIRONNEMENT INFORMATIQUE

Le code source de cette évaluation est disponible à l'adresse suivante:

<https://github.com/pbs-assess/arrowtooth>

Cette version du document a été générée sur `r lubridate::as_date(Sys.time())` avec
`r devtools::session_info()$platform$version` [@r2022] et les versions du paquet R:

")
```

```{r env, echo = FALSE, results='asis', message=FALSE, warning=FALSE, eval=TRUE, cache=FALSE}

# Need to load sdmTMB here for it to show up in the packages list with version number
library(sdmTMB)
library(gfutilities)

pkgs <- sort(c("arrowtooth",
               "bookdown", "broom",
               "csasdown",
               "devtools", "dplyr",
               "gfdata", "gfiscam", "gfiscamutils", "gfplot", "ggplot2", "glmmTMB", "gridExtra",
               "here",
               "kableExtra",
               "knitr",
               "purrr",
               "rmarkdown", "rosettafish",
               "sdmTMB",
               "tidylog", "TMB"))

info <- session_info()$packages |> 
  as.data.frame() |> 
  as_tibble() |> 
  select(package, loadedversion) |> 
  filter(package %in% pkgs) |> 
  setNames(c(tr("Package"), tr("Version"))) |>
  csas_table()

info
```

```{r app-comp-env-para-2-en, eval = !fr(), results = 'asis'}
cat("\texttt{gfiscam} is an ADMB/C++ package containing the `r iscam` implementation used in this assessment and has a GitHub repository (link below).

Packages maintained by the authors used to generate this report can be viewed at:

")
```

```{r app-comp-env-para-2-fr, eval = fr(), results = 'asis', needs_trans = FALSE}
cat("\texttt{gfiscam} est un paquetage ADMB/C++ contenant l'implémentation de `r iscam` utilisée dans cette évaluation et dispose d'un dépôt GitHub (lien ci-dessous).

Les paquets maintenus par les auteurs utilisés pour générer ce rapport peuvent être consultés à l'adresse suivante :
")
```

https://github.com/pbs-assess/arrowtooth

https://github.com/pbs-assess/csasdown

https://github.com/pbs-assess/gfdata

https://github.com/pbs-assess/gfiscam/wsl2

https://github.com/pbs-assess/gfiscamutils

https://github.com/pbs-assess/gfplot

https://github.com/pbs-assess/gfutilities

https://github.com/cgrandin/kableExtra

https://github.com/pbs-assess/rosettafish

https://github.com/pbs-assess/sdmTMB

\clearpage
