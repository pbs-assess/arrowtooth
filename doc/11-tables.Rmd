# Tables

```{r overall-catch-table, results = "asis"}
cap <- paste0("Recent coastwide commercial fishery catch (t) for ",
              sp, " by gear type.")
if(fr()){
cap <- paste0("Récentes captures (t) de la pêche commerciale côtière ",
              "de ", sp, " par type d'engin de pêche.")
}

# Table function not finished
# table_catch(catch_df,
#             gear_rm = c("trawl", "LONGLINE"),
#             digits = 1,
#             caption = cap,
#             start_yr = 1996,
#             font_size = 8,
#             gear_col_widths = "6em")
```
